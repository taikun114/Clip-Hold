# AGENTS

## プロジェクトについて

プロジェクト名: **Clip Hold**

ターゲットプラットフォームは次の通り。

- macOS 14.0またはそれ以降

使用されているパッケージは次の通り。

- **KeyboardShortcuts**
  - グローバルショートカットキーを設定・管理するためのライブラリ。定型文やコピー履歴アイテムの呼び出しショートカットや、ウィンドウを開くショートカット等に使用している。
- **UniversalSFSymbolsPicker**
  - SwiftUI用のSF Symbols選択ピッカー。プリセット用のアイコン選択機能に使用している。
- **HighlighterSwift**
  - ソースコードのシンタックスハイライト（highlight.js の Swift ラッパー）を提供するライブラリ。クイックオーバーレイ等のツールチップでのコードハイライトに使用している。

### プロジェクト概要

**Clip Hold**は、macOS用のクリップボードおよび定型文（Standard Phrase）マネージャーアプリである。
頻繁に使用する定型文をプリセットとして管理したり、コピー履歴を一覧から素早く呼び出したりできる。また、特定のアプリごとにプリセットを自動で切り替える機能や、コピー後自動でペーストする「クイックペースト」機能、パスワード等を一時的に保存しないための監視一時停止機能・除外アプリ設定機能などを備えている。
SwiftUIで構築されており、メニューバー常駐型のエクスペリエンスを提供する。

## 開発方針と注意事項について

### 開発方針

#### ローカライズに関して

- 「定型文」を英語で記載する場合は、**standard phrase**（複数形は **standard phrases**）に統一する。
- 「項目を変更してコピー」を英語で記載する場合や変数名などにする場合は、**Change Item and Copy** （あるいは `changeItemAndCopy`）に統一する。
- 「クイックペースト」および「クイックオーバーレイ」をアルファベット（英語）で表記する場合は、機能名として扱うため、**Quick Paste** および **Quick Overlay** のようにタイトルケース（Title Case）に統一する。
- **`Localizable.xcstrings` の更新について**:
  - コード内に追加した新しい翻訳キーを `Localizable.xcstrings` に反映（再インデックス）させるには、XcodeのMCPツール（`BuildProject`）またはXcodeのGUIでビルドを行う必要がある。
  - シェルコマンド（`xcodebuild`など）でビルドしただけでは新しいキーがファイルに認識されないため注意すること。MCPツールが利用できない環境では、ユーザーにGUIでのビルドを依頼するか、手動でキーを追加して対応すること。
  - 新たに追加されたキーを確認する場合は、`Localizable.xcstrings` ファイルに対して `git diff` を行うことで確認できる。

#### 表記について

- 日本語では、「〜するとき」などの形式名詞としての「とき」はひらがなで統一する（「〇〇時」などの特定の動作や時刻を表す場合は漢字を使用する）。

#### デバッグログについて

- ログ出力には `print` を使用する。
- 詳細な挙動を確認するためのログは `#if DEBUG` で囲み、リリースビルドには含まれないようにする。
- ただし、リリース後にユーザーから報告された不具合の原因を特定しやすくするため、重要な状態の変化やエラーの発生に関するログは `#if DEBUG` で囲まず、リリースビルドでも出力されるように一部残すこと。

#### パフォーマンスと応答性について

- アプリの動作を軽量に保つため、可能な限り処理をバックグラウンドスレッドで実行し、UIのフリーズを回避（または最小限に抑制）するように実装すること。

#### 新機能の実装について

- 新機能を実装する際やリファクタリングを行う際、今後の開発持続性やメンテナンス性を高めるため、可能な限り再利用可能なコンポーネントを作成して使用すること
- あらゆるユーザーが簡単にClip Holdを使用できるよう、様々なアクセシビリティに配慮した設計・実装を行うこと
  - 例えば、レイアウトの関係により、ラベルを非表示に（`.labelsHidden()`などを付与）したスイッチやピッカーでもVoiceOverで読み上げられるように、ラベルテキストを設定すること

#### テストの実装について

- 普段使いしているアプリ環境の実データ（履歴、定型文、プリセット、除外アプリ設定、UserDefaults等）に影響を与えないよう、**破壊的な変更（データの削除やリセット等）を伴うテストは行わない**こと。
- テストは可能な限りメモリ上（独立したインスタンス）で完結するロジックやデータモデルの検証にとどめること。
- やむを得ずシングルトンや共有状態（ピン留め状態など）を変更・操作するテストを行う場合は、テスト開始前の状態を必ず退避し、`defer` ブロック等を用いてテスト終了時（成否問わず）に確実に元の状態へ復元すること。
